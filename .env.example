# Environment Variables for Tic-Tac-Toe Multiplayer
# Copy this file to .env and update values as needed
# For Coolify deployment, set these in the Coolify dashboard

# Node Environment
NODE_ENV=production

# Server Port (default: 5000)
PORT=5000

# CORS Allowed Origins
# In production, leave empty or set to '*' to allow all origins
# For specific domains, use comma-separated list: https://example.com,https://www.example.com
ALLOWED_ORIGINS=

# React App Socket URL
# Leave empty to use window.location.origin (same domain)
# Only set if socket server is on a different domain
REACT_APP_SOCKET_URL=
